# ==============================
# Production Configuration
# ==============================

# App Port (used automatically by Railway)
server.port=${PORT:8080}

# MySQL Database (Railway Public Endpoint)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://interchange.proxy.rlwy.net:19540/railway}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:ArJaZWOtvBNEkrHAtHqdvYPQtDcJWjxo}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Multipart Upload Limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Project Image Storage Path
project.image=images/

# Logging (Spring Security)
logging.level.org.springframework.security=info

# Spring Security Basic Auth (used if JWT not configured)
spring.security.user.name=${SPRING_SECURITY_USER_NAME:atharva}
spring.security.user.password=${SPRING_SECURITY_USER_PASSWORD:atharva}
spring.security.user.roles=${SPRING_SECURITY_USER_ROLES:ADMIN}
